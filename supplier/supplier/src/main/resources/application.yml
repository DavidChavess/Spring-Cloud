server:
  port: 8081

spring:
  application:
    name: 'fornecedor'
  datasource:
    url: jdbc:mysql://192.168.99.105:3306/supplier
    username: root
    password: senha123
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
